/**
 * @module nestguard/jwt
 *
 * Optional JWT helper for NestGuard.
 * Provides utilities for the permissionsVersion-in-JWT staleness strategy.
 *
 * Public surface (populated in a future phase):
 *   - extractSubjectFromJwt()
 *   - verifyPermissionsVersion()
 */

// Populated in a future phase — nothing to export yet.
export {};

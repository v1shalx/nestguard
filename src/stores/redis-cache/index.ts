/**
 * @module nestguard/redis
 *
 * Optional Redis cache layer for NestGuard.
 * Wraps any SubjectResolver and caches resolved permission sets
 * with TTL + version-based invalidation.
 *
 * Public surface (populated in Phase 4):
 *   - RedisCachedResolver
 *   - redisCache() (factory)
 */

// Populated in Phase 4 — nothing to export yet.
export {};

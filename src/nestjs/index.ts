/**
 * @module nestguard/nestjs
 *
 * NestJS adapter for the NestGuard permission engine.
 *
 * Public surface (populated in Phase 2):
 *   - NestGuardModule (forRoot / forRootAsync)
 *   - NestGuard (CanActivate guard)
 *   - @RequirePermissions, @Public, @Authenticated, @CurrentSubject (decorators)
 */

// Populated in Phase 2 — nothing to export yet.
export {};
